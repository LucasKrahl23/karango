<%= form_for(formaspagamento) do |f| %>
  <% if formaspagamento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(formaspagamento.errors.count, "error") %> prohibited this formaspagamento from being saved:</h2>

      <ul>
      <% formaspagamento.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %>
    <%= f.text_field :nome %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
