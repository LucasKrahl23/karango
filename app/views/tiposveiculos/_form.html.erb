<%= form_for(tiposveiculo, :html => { :class => 'form-horizontal' }) do |f| %>
  <% if tiposveiculo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tiposveiculo.errors.count, "error") %> prohibited this tiposveiculo from being saved:</h2>

      <ul>
      <% tiposveiculo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row example-row">
    <div class="col-md-3">Nome:</div> 
      <div class="col-md-9">
        <div class="inputer">
            <div class="input-wrapper">
              <%= f.text_field :nome, class: 'form-control' %>
            </div>
          </div>
      </div> 
  </div>

  <div class="form-buttons">
    <div class="row">
      <div class="col-md-offset-3 col-md-9">
        <%= f.submit 'Salvar', :class => 'btn btn-blue btn-ripple' %>
        <input type="reset" value="Limpar Campos" class="btn btn-flat btn-danger btn-ripple">
        <a href="<%= tiposveiculos_path %>" class="btn btn-flat btn-ripple">Voltar</a>
      </div>
    </div>
  </div>
<% end %>
