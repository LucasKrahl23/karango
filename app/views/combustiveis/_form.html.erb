<%= form_for(combustivel) do |f| %>
  <% if combustivel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(combustivel.errors.count, "error") %> prohibited this combustivel from being saved:</h2>

      <ul>
      <% combustivel.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %>
    <%= f.text_field :nome %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
